# Environment Variables
# This is a client-side-only application — no environment variables are required.
# All VITE_* variables are exposed to the browser via import.meta.env.VITE_*

# Optional: customize the application title
VITE_APP_TITLE=HireHub